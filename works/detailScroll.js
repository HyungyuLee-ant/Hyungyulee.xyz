//not used

$(document).ready(function () {
    // alert('yes')
    $('.content').scroll(function () {
        // alert('yes')
        if ($(window).width() > 1000) {
        // $('.title').css('position', 'fixed')
        // $('.title').css('width', '48vw')
        // $('.title').css('bottom', '124px')
            // $('.title').css('position', 'relative').fadeIn(800)
        }
    })

})